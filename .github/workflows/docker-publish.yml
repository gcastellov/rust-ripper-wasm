name: Docker

on:
  push:
    branches:
      - staging
    tags:
      - v*
  workflow_run:
    workflows: ["Node.js CI"]
    branches:
      - staging
    types:
      - completed

env:
  IMAGE_NAME: rustripperwasm

jobs:

  push:    

    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - uses: actions/checkout@v2

      - name: Build image
        run: docker build -f ./docker/Dockerfile -t $IMAGE_NAME .

      - name: Log into registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login docker.pkg.github.com -u ${{ github.actor }} --password-stdin

      - name: Push image
        run: |
          IMAGE_ID=docker.pkg.github.com/${{ github.repository }}/$IMAGE_NAME

          # Change all uppercase to lowercase
          IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')

          # Strip git ref prefix from version
          VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')

          # Strip "v" prefix from tag name
          [[ "${{ github.ref }}" == "refs/tags/"* ]] && VERSION=$(echo $VERSION | sed -e 's/^v//')

          # Use Docker `latest` tag convention
          [ "$VERSION" == "main" ] && VERSION=latest

          echo IMAGE_ID=$IMAGE_ID
          echo VERSION=$VERSION

          docker tag $IMAGE_NAME $IMAGE_ID:$VERSION
          docker push $IMAGE_ID:$VERSION
          
      - name: Call deployment webhook
        run: | 
          USERNAME=${{secrets.AZURE_WEBHOOK_USERNAME}}
          TOKEN=${{secrets.AZURE_WEBHOOK_PWD}}
          URL=${{secrets.AZURE_WEBHOOK_URL}}
          curl -X POST -u $USERNAME:$TOKEN $URL -d -H > /dev/null

